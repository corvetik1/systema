import React from 'react';
import { Typography } from '@mui/material';

const UserManagementPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">User Management Page</Typography>
    </div>
  );
};

export default UserManagementPage;